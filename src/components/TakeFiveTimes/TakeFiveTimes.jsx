//This function's intended purpose is to take the array of 40 items and refine
//it down to five times at noon

function TakeFiveTimes(newArray) {
  const returnedArray = ['item 0'];
  return returnedArray;
}

export default {
  TakeFiveTimes
};
